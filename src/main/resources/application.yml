server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/web?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: tm98fm56dl0@

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
